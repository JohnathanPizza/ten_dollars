>SERVICE_CYCLE_BEGIN
	CLI
	JSR	SCHEDULER_INCREMENT
	JSR	SCHEDULER_SELECT
	LDA.Z	VR11
	; A has procid to serve
	STA	PROC_ACTIVE_ID
	TAX
	STZ.X	PROC_SCHEDULE_COUNT
	; jump to function based on process state
	LDA.X	PROC_STATE
	ASL
	TAX
	JMP.XP	SERVICE_ARRAY
