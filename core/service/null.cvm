; resets the current active process priority to 0 so that it cant be scheduled again really
; if you end up here then there is probably no process doing anything

>SERVICE_NULL
	LDX	PROC_ACTIVE_ID		; load process id
	STZ.X	PROC_SCHEDULE_PRIORITY	; set process priority to 0
	JMP	SERVICE_CYCLE_BEGIN	; start next cycle
